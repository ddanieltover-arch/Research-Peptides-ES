/** Footer brand mark — cream ESPAÑA horizontal logo for dark backgrounds. */
import footerLogo from './footer-logo.webp';
import { assetUrl } from '../lib/assetUrl';

export default assetUrl(footerLogo);
